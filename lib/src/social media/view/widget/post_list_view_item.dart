import 'package:flutter/material.dart';

class PostListViewItem extends StatelessWidget {
  const PostListViewItem({super.key});

  @override
  Widget build(BuildContext context) {
    return Placeholder();
  }
}
